import Journal from '../components/journal/_index'

// This is just rendering the component for now.
export default function journaltemp() {
  return (
    <div style={{ height: '100vh', width: '100vw', background: 'grey' }}>
      <Journal />
    </div>
  )
}

